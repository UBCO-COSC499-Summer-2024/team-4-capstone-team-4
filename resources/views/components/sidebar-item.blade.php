<div class="sidebar-item">
    {{-- link component --}}
    <x-link
        class="sidebar-link"
        type="{{ $type }}"
        icon="{{ $icon }}"
        page="{{ $route }}"
        >
    </x-link>
</div>
